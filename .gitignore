# Dependencies
node_modules/

# Build output
dist/

# Environment files (NEVER commit these)
.env.local
.env.local.test
.env.*.local

# Editor / OS
.DS_Store
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
*.log
npm-debug.log*

# Test results
test-results/
playwright-report/
.playwright-mcp/

# Vite cache
.vite/

# TypeScript
*.tsbuildinfo

# Cartelle locali di lavoro (mai committate)
Lavoro/
Immagini-sfondo/
icone-nuove/

# Lavoro privato personale (note, materiale didattico) — mai versionato, neanche in dev
docs/_lavoro/

# Harness agenti locali: vive nella repo privata TestingAgentHarness, non qui.
agenti-locali/

# Artefatti Vercel locali (project link) — mai versionati
.vercel/

# Piani di lavoro scratch di Cursor — temporanei, mai versionati
.cursor/plans/

# Script Playwright usa-e-getta (debug locale, mai committati)
pw-*.mjs

# Note di lavoro sciolte in root
/Report idea workflow per sviluppatore

# Pagine HTML salvate dal browser (es. export Supabase Dashboard)
/*.html
/*_files/

# Artefatti Python (script di analisi in docs/)
__pycache__/
*.pyc

# Archivi storici — non sono MAI stati tracciati (PIANO_INDAGINE §2.3 p.2).
# Contengono, fra l'altro, "Crescita professionale/": copia byte-identica di
# Documents\Io-Claude\Crescita professionale con dati anagrafici + un .git annidato.
docs/Archives/

# Claude Code locale: permessi/path assoluti personali (settings.local.json) e
# config MCP con chiavi (mcp.json) — personali alla macchina, mai versionati.
# Il blocco hooks condiviso vive in .claude/settings.json (tracciato, git add -f).
.claude/settings.local.json
.claude/mcp.json
