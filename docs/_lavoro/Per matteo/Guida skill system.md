Se dici «sistema», «implementa», «aggiungi», «crea» (o simili) → l'agente entra in Esecuzione: carica solo l'area che tocchi + grafica, salta testing/debug.


Se dici «revisiona», «controlla», «verifica», «debugga», «non funziona» → entra in Verifica: carica i test + l'area che sta controllando.


Se dici «migliora comunicazione» o «aggiorna comunicazione» → entra in Meta: carica solo le due skill di comunicazione, niente codice.

Follow-up rimandati dopo una sessione (controlli che non hai fatto subito) → `docs/FOLLOW_UP.md`, collegati al report in Sessioni di lavoro. L’agente «prepara prompt» li cerca anche se non li chiedi tu.