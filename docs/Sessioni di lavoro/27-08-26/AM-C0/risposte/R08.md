Devo rispondere alla domanda sulle soglie di ritardo tavolo, la durata dei walk-in e i tempi tra clienti nell'applicazione ristorante.

Devo seguire un formato rigoroso: dichiarare prima le righe di contesto, poi rispondere alle domande e infine fornire piano, provenienza e stop se necessario. Esplorerò il codice per trovare le soglie di ritardo tavolo, la durata dei walk-in e i tempi tra clienti, verificando anche se questi parametri sono configurabili dall'app.

Ho verificato il contesto operativo: cartella di lavoro attuale, memoria persistente assente, nessun file esterno letto, modello agente e strumenti disponibili. Ora cerco nel codice le definizioni di ritardo tavolo, walk-in e tempi tra clienti.

Cartella di lavoro: c:\tmp\amc0\0508-oggi. Cerco nel codice i valori di ritardo tavolo, durata walk-in e intervallo tra clienti.

Cartella di lavoro: c:\tmp\amc0\0508-oggi

Memoria persistente caricata: no — nessuna memoria Cursor pre-caricata per questo progetto in questa sessione.

File letti fuori da questa cartella: nessuno.

Conoscenza pregressa di questo progetto: sì — limitata alle regole di workspace (AGENTS.md, comandi-base.mdc) e alle skill elencate al boot; i valori numerici e la configurabilità sono stati verificati nei file sotto questa cartella.

Modello e versione: Auto (agent router Cursor).

Strumenti disponibili: lettura file, grep, ricerca nel workspace (modalità Ask — sola lettura, niente write/shell che modifica stato).

Risposta per Mario (in parole semplici)
Mario usa la vista Servizio (Pro): sulla piantina dei tavoli ogni tavolo ha un colore che cambia con l’orologio (aggiornamento circa ogni 30 secondi).

1. «Dopo quanti minuti segna In ritardo?»
15 minuti dopo l’orario di arrivo previsto, se il cliente non è ancora considerato «fuori dal pasto».

La logica è in useTableStatuses.ts: da «In arrivo» passa a «Occupato» all’arrivo; resta «Occupato» per la soglia di grazia; poi diventa «In ritardo» fino alla fine della finestra di occupazione.

Default: 15 minuti (DEFAULT_LATE_THRESHOLD_MINUTES = 15, riga 35).
Il valore può essere diverso se nel database, tabella restaurant_settings, chiave JSONB table_late_threshold_minutes, è salvato un numero (0–120).
Mario oggi non può cambiarlo dall’app: esiste il registry in codice, ma nessun campo in Admin/Servizio/Impostazioni per quella chiave (grep su src/ trova solo hook + registry, non UI).

2. «Quanto dura in automatico un walk-in?»
90 minuti dalla creazione, in quasi tutti i casi reali.

Quando Mario (o lo staff) aggiunge un walk-in dal modale «Aggiungi walk-in», l’app calcola confirmed_end = ora attuale + durata. Il resolver di durata (resolveBookingDuration) per il walk-in riceve solo il «pavimento» della fascia (min_duration); senza card/tipologia/menu non produce una durata → scatta il fallback:


useWalkInMutation.ts
Ln 123–127
} else {
  // Permanenza OFF (D42): nessuna durata configurata → fallback a 90 min.
  // Non persiste snapshot perché non c'è una sorgente da tracciare.
  durationMinutes = 90
}
Il modale stima la capienza con min_duration della fascia o 90 min, ma la durata effettiva salvata segue la regola sopra (90 min se il resolver non risolve).

Mario non può impostare la durata del walk-in dall’app: in Servizio c’è solo il limite di coperti per walk-in (walk_in_max_guests, card «Limite walk-in»), non i minuti.

3. «Quanto tempo lascia fra un cliente e il successivo?»
Qui servono due concetti diversi:

Cosa	Dove	Default oggi	Cosa fa per Mario
Buffer di riassetto (turnover)
Colonna turnover_buffer_minutes su ogni fascia (service_slots)
0 minuti (mig. 057)
Minuti dopo la fine del pasto (confirmed_end) prima che il tavolo sia libero per il turno successivo e prima che lo stato passi a «In uscita». Fine occupazione = fine pasto + buffer (resolveOccupancy.ts, useTableStatuses.ts).
Intervallo di arrivo
arrival_step_minutes sulla fascia
spesso 30 (configurabile)
Non è il gap tra due turni sullo stesso tavolo: è ogni quanto il cliente può scegliere l’orario sul form Prenota (15 / 30 / 60 min o personalizzato 5–120). Campo «Intervallo di arrivo» nel gestore fasce in Servizio (ServiceSlotsManager.tsx, righe ~699–739).
Per «fra un cliente e il successivo sullo stesso tavolo» la risposta corretta è il buffer turnover (oggi 0 se non impostato nel DB). Due prenotazioni consecutive sullo stesso tavolo non si sovrappongono se la prima finisce esattamente quando la seconda inizia (intervallo semi-aperto [start, end) in resolveOccupancy.ts).

Mario può cambiare l’intervallo di arrivo (orari selezionabili su Prenota) da Servizio → Fasce orarie.
Non può cambiare il buffer turnover dall’app: colonna e hook ci sono, ma nessun campo nel form delle fasce (stato documentato in GUIDA_CONFIGURAZIONE_CLIENTE.md riga 193: 🔜, solo operatore).

4. «Posso cambiarli io dall’app?»
Parametro	Mario dall’admin	Note
Soglia «In ritardo» (15′ default)
No
Solo DB / console futura
Durata walk-in (90′)
No
Solo codice; follow-up FU-SERV-ADMIN-PANEL-1
Buffer fra turni sul tavolo
No
DB; UI non esposta
Intervallo orari su Prenota
Sì
Servizio → modifica fascia → «Intervallo di arrivo»
Max coperti walk-in
Sì
Servizio → card limite walk-in
Orari fasce, turni max, coperti fascia
Sì
Gestore fasce Servizio
Servizio e walk-in richiedono edition Pro; Classic non ha la voce Servizio (COLLAUDO_S4_CHECKLIST.md §7).

1. IL PIANO
Contesto: Mario chiede valori e controllo. Il motore già legge soglia ritardo e buffer da DB/registry; i default sono 15′ ritardo, 90′ walk-in, 0′ buffer. Manca l’UI per tre manopole; la checklist S4 (§10) le segna ancora come decisioni aperte con Matteo.

Obiettivo: permettere a Mario (Pro) di configurare dall’admin ciò che oggi è solo default/codice, senza cambiare la logica già in useTableStatuses / useWalkInMutation / resolveOccupancy.

Ordine di esecuzione (per agente in modalità Esecuzione):

Caricare contesto

docs/APP_CONTEXT_SKILL.md §0 → Admin/Servizio.
docs/Admin-Skill/contesto/ADMIN_SERVIZIO_CONTEXT.md §9.1.
docs/Servizio-Config/GUIDA_CONFIGURAZIONE_CLIENTE.md tabella manopole.
Conferma prodotto con Matteo (gate umano, non codice)

Default proposti: ritardo 15′, buffer turnover 10′ per Pro (come in COLLAUDO_S4_CHECKLIST.md §10), walk-in 90′ o allineato a min_duration fascia?
Dove in UI: card in Servizio (vicino fasce/walk-in) vs Impostazioni?
UI — soglia «In ritardo»

File: nuovo componente es. ServizioTimingSettingsCard.tsx in src/features/booking/components/servizio/, montato in ServizioPage.tsx (stesso pattern di WalkInLimitCard.tsx).
Chiave: table_late_threshold_minutes via useRestaurantSetting + useUpsertRestaurantSetting.
Validazione: 0–120 intero (restaurantSettingRegistry.ts righe 784–787).
Label utente: «Minuti dopo l’arrivo prima di segnare In ritardo».
Test: unit sul registry (già presente per recall); component test con mock hook.
UI — buffer turnover + min_duration per fascia

File: ServiceSlotsManager.tsx — aggiungere campi numerici accanto a «Intervallo di arrivo».
Persistenza: useServiceSlots già espone min_duration, turnover_buffer_minutes (useServiceSlots.ts).
Label buffer: «Minuti di pulizia dopo il pasto prima del turno successivo».
Label min_duration: «Durata minima pasto (pavimento)» — chiarire che non cambia il walk-in da sola (resolver D35).
Validazione: turnover_buffer_minutes >= 0; min_duration allineata a BOOKING_DURATION_MIN/MAX se applicata.
Test: estendere serviceSlots.sovrapposizione.test.tsx o blindatura Servizio.
(Opzionale, se Matteo conferma) — durata walk-in configurabile

Opzione A: nuova chiave walk_in_default_duration_minutes in restaurantSettingRegistry.ts + card UI + uso in useWalkInMutation invece del literal 90.
Opzione B: far passare al walk-in un livello del resolver (es. booking_mode_default_duration della tipologia «tavolo») — più coerente con D35 ma più invasivo.
Test: walkIn.b2.test.tsx — aggiornare casi fallback.
Documentazione skill (stesso ciclo se il diff cambia comportamento descritto)

GUIDA_CONFIGURAZIONE_CLIENTE.md righe 192–193: stato da 🔜 a ✅ e 🟩 cliente dove applicabile.
ADMIN_SERVIZIO_CONTEXT.md: dove Mario trova le manopole.
COLLAUDO_S4_CHECKLIST.md §10: chiudere le tre voci aperte.
Verifica

npm run lint, npm run typecheck, test area Servizio.
Manuale Pro: Servizio → imposta ritardo 5′ → prenotazione con arrivo passato → dopo ~5′ + refresh clock 30s → «In ritardo».
Walk-in → confirmed_end coerente con durata scelta.
Fascia con buffer 15′ → stato «In uscita» / capienza tavolo 15′ dopo confirmed_end.
NON fare in questo piano

Migrazioni DB per le chiavi JSONB (non servono).
Console super-admin (FU-SERV-ADMIN-PANEL-1) — parallelo, non sostituto se l’obiettivo è self-service Mario.
Se Matteo decide che i default bastano e Mario non deve configurare: piano = STOP implementazione; solo aggiornare documentazione cliente/help con i tre numeri (15 / 90 / 0) e chiarire intervallo arrivo vs buffer turnover.

2. LA CARD DI PROVENIENZA
Azione proposta o STOP:
STOP implementazione immediata fino a gate prodotto (default buffer 10′? walk-in sempre 90′? dove in UI?). Informare Mario con i valori verificati in codice; piano sopra solo se Matteo vuole self-service in admin.

Perché agisco così:
Mario chiede numeri e controllo. I numeri sono nel codice; il controllo è parziale (solo intervallo arrivo e limite coperti walk-in). Intervenire senza conferma rischia UI incoerente con decisioni ancora aperte in collaudo S4.

Decisione/fonte citata:

Ritardo 15′ default + lettura table_late_threshold_minutes: src/features/booking/hooks/useTableStatuses.ts righe 34–35, 144–147, 170–188.
Stati «Occupato» → «In ritardo»: stesso file righe 8–13, 144–147.
Walk-in 90′ fallback: src/features/booking/hooks/useWalkInMutation.ts righe 102–127.
Buffer turnover default 0, fine occupazione = end + buffer: supabase/migrations/057_service_slots_duration_buffer.sql righe 7–8; src/features/booking/lib/resolveOccupancy.ts righe 44–46, 52–63; useTableStatuses.ts righe 77–96.
Intervallo arrivo UI: ServiceSlotsManager.tsx righe 699–739.
Walk-in max coperti UI: WalkInLimitCard.tsx + chiave walk_in_max_guests.
Manopole non in UI admin: assenza match table_late_threshold / turnover_buffer in componenti UI oltre hook; GUIDA_CONFIGURAZIONE_CLIENTE.md righe 192–193.
Decisioni aperte: docs/Testing-Skill/COLLAUDO_S4_CHECKLIST.md righe 282–285.
S-5 «nessuna UI adesso» per recall 30′: docs/Sessioni di lavoro/03-08-26/PROMPT_FASE0_QUATTRO_FIX.md righe 107–110.
Condizioni che coincidono:
Tenant Pro con Servizio; soglia letta da JSONB o default 15; walk-in senza gerarchia durata D35 → 90′; fasce senza turnover_buffer_minutes impostato → 0; arrival_step_minutes separato dal gap tra turni.

Informazione che manca o confligge:

Buffer «ufficiale» per Pro: checklist propone 10′ (COLLAUDO_S4_CHECKLIST.md riga 283), codice default 0 (mig. 057).
Walk-in vs min_duration fascia: UI stima con min_duration ?? 90 (WalkInModal.tsx righe 49–51), mutation salva 90 se resolver undefined — possibile confusione staff.
Edition: Classic non ha Servizio; risposta valida solo Pro.
Prossimo passo sicuro:
Rispondere a Mario con 15′ / 90′ / 0′ buffer (e distinguere intervallo arrivo). Chiedere a Matteo: (1) default buffer 0 o 10 per nuovi Pro; (2) se Mario deve cambiare ritardo, buffer e durata walk-in dall’admin → eseguire piano §1; (3) se basta documentazione → aggiornare guida cliente, nessun codice.

3. LO STOP
STOP (per implementazione, non per la risposta a Mario):

Buffer di riassetto default per i clienti Pro: 0 (codice) o 10 minuti (proposta collaudo)? Senza questa risposta non si può decidere se solo esporre UI, cambiare default DB, o entrambi.

Mario deve configurare da solo ritardo / buffer / durata walk-in, o per ora basta che lo staff/matteo imposti via DB/console? Se «basta sapere i numeri», il piano UI è fuori perimetro.

Perché senza queste risposte non si può andare avanti: la checklist S4 tratta proprio queste tre manopole come non confermate; il codice ha già chiavi e logica ma default e self-service non sono allineati al prodotto. Implementare UI senza gate ripete il rischio documentato in PROMPT_FASE0_QUATTRO_FIX.md (S-5: manopole senza migrazione ma con scelta esplicita su dove e se mostrarle).

Per rispondere a Mario oggi: nessuno STOP — i valori attuali sono verificabili nel repo; le limitazioni di configurazione sono verificabili dall’assenza di UI in src/.