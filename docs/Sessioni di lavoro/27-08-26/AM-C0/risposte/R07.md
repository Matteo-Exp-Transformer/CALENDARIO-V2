Devo seguire il formato richiesto per rispondere al caso sui parametri di capienza del ristorante.

Ho individuato la cartella di lavoro: c:\tmp\amc0\1706-storica. Esplorerò il codice per capire come funzionano i limiti di prenotazione. Poi preparerò il piano di configurazione della capienza e la card di provenienza.

Cartella di lavoro: c:\tmp\amc0\1706-storica. Esploro il codice per capire come funzionano i limiti di capienza (giorno intero e fasce orarie).

Cartella di lavoro: c:\tmp\amc0\1706-storica

Memoria persistente caricata: sì — regole workspace (.cursor/rules/comandi-base.mdc, AGENTS.md), skill pointer Prenota/Admin/DB, nessuna memoria di sessioni precedenti su Mario o su questo tenant specifico.

File letti fuori da questa cartella: nessuno

Conoscenza pregressa di questo progetto: sì — conoscenza generica del repo CalendarBackup-v2 (Prenota pubblica, admin Impostazioni, limiti coperti); nessun dato runtime sul tenant di Mario.

Modello e versione: Composer (agente Cursor)

Strumenti disponibili: lettura file, grep/glob, ricerca web (non usati), MCP Supabase/Playwright/browser (non usati — sola lettura codice)

1. IL PIANO
Contesto prodotto (da comunicare a Mario in parole semplici)
Mario vede due caselle diverse con ruoli diversi:

Casella	Dove sta	Cosa fa verso il sito pubblico
Coperti max al giorno
Admin → Impostazioni locale (blocco dedicato, sotto Orari di apertura)
Blocca le nuove richieste dal sito quando il totale coperti già accettati per quel giorno supererebbe il limite
Coperti max per fascia
Classic: stesso tab Impostazioni → sezione «Imposta Fasce Orarie» · Pro: Servizio → Fasce orarie
Non blocca il sito: è un semaforo interno (calendario, richieste in attesa, form admin). L’help in UI lo dice esplicitamente
La decisione prodotto è documentata: il blocco per-fascia sul pubblico è disattivato (slot_limit_enabled default false); resta solo il limite giornaliero (ADMIN_PRENOTAZIONI_CONTEXT.md punti 10–11).

Fase A — Prima di toccare qualsiasi cosa (5 min)
Identificare il tenant di Mario (slug, email admin, edition Classic vs Pro).

Classic: vede «Imposta Fasce Orarie» in Impostazioni (RestaurantSettingsTab.tsx L1375: visibile solo se !features.servizio).
Pro: le fasce si gestiscono in Servizio; in Impostazioni resta comunque «Coperti max al giorno».
Leggere lo stato attuale in DB (solo TEST, o PROD con conferma Matteo):

SELECT setting_key, setting_value
FROM restaurant_settings
WHERE tenant_id = '<tenant_id>'
  AND setting_key IN (
    'daily_guest_limit',
    'slot_guest_capacities',
    'booking_time_slots_enabled',
    'slot_limit_enabled'
  );
Più le fasce:

SELECT id, name, start_time, end_time, max_guests, display_order
FROM service_slots
WHERE tenant_id = '<tenant_id>'
ORDER BY display_order;
Verificare che l’edge create-booking deployata contenga il blocco DAILY_LIMIT (supabase/functions/create-booking/index.ts L349–366). Senza deploy, il limite giornaliero in Impostazioni non ha effetto sul pubblico.

Fase B — Istruzioni operative per Mario (configurazione)
Obiettivo: far sì che i clienti dal sito non prenotino oltre la capienza.

B1 — Limite giornaliero (unico blocco automatico sul pubblico)
Accedere ad Admin → tab Impostazioni (AdminDashboard.tsx L359, settings-restaurant).
Scorri fino al blocco «Coperti massimi al giorno» (RestaurantSettingsTab.tsx L1329–1372).
Inserire il numero totale di coperti servibili in quel giorno (es. 80).
Vuoto = nessun limite.
0 viene trattato come illimitato (restaurantSettingRegistry.ts L414–418).
Min 1, max 1000 in UI.
Cliccare Salva modifiche (footer) e confermare la modale dati pubblici se compare.
Spiegare a Mario:
Contano solo le prenotazioni accettate (status = 'accepted'), non quelle in attesa.
I no-show non occupano posto (liberano capienza).
Le richieste pending non contano verso il tetto: più clienti possono inviare richieste finché non accetti abbastanza da saturare il giorno; il blocco scatta al submit successivo sul sito.
B2 — Capienze per fascia (organizzazione interna, non blocco sito)
Classic (!features.servizio):

Nella stessa pagina Impostazioni, sezione «Imposta Fasce Orarie».
Attivare il checkbox Attiva / Disattiva se le fasce devono raggruppare il calendario.
Per ogni fascia (Pranzo, Cena, …), compilare Coperti max (RestaurantSettingsTab.tsx L1548–1569).
Aprire l’help «?» e ribadire il testo UI (L1419–1423): «riferimento… non blocca nulla in automatico».
Salva → persiste in restaurant_settings.slot_guest_capacities (mappa slot.id → numero).
Pro (features.servizio):

Servizio → Fasce orarie → modifica fascia → campo coperti massimi (ServiceSlotsManager.tsx L680–692).
Stesso messaggio: semaforo/avviso, non rifiuta dal sito.
B3 — Come Mario usa il semaforo per-fascia nella pratica
Calendario: badge % sul giorno se c’è daily_guest_limit; digest per fascia con disponibilità.
Prenotazioni → In attesa: contatore disponibili/capacità per fascia (CollapsibleCard.tsx, PendingRequestsTab.tsx).
Nuova prenotazione admin: CapacityWarningModal se supera cap fascia o giorno — ma l’admin può sempre procedere dopo conferma (ADMIN_PRENOTAZIONI_CONTEXT.md punti 14, 17).
Fase C — Verifica end-to-end (obbligatoria prima di chiudere il caso)
Su staging/tenant Mario, impostare daily_guest_limit a un valore basso (es. 10) e salvare.
Creare o avere prenotazioni accettate per un giorno test con somma coperti = 8.
Dal sito /prenota/:slug, inviare richiesta per 4 persone stesso giorno → deve fallire con toast: «Spiacenti, abbiamo raggiunto il numero massimo di coperti per questa data.» (edge create-booking L358–363, codice DAILY_LIMIT).
Ripetere con 2 persone → deve passare (8+2=10, al limite).
Confermare che compilare Coperti max su una fascia non impedisce il submit pubblico (comportamento atteso).
Opzionale admin: creare prenotazione oltre il limite giornaliero → deve comparire CapacityWarningModal, submit consentito dopo «Procedi».
Fase D — Se Mario chiede «voglio che anche la fascia blocchi il sito»
Non è il comportamento standard oggi. Percorso tecnico documentato ma non esposto in UI:

Impostare restaurant_settings.slot_limit_enabled = true per il tenant (solo con decisione prodotto Matteo).
L’edge usa allora service_slots.max_guests (o override giornaliero service_slot_overrides) — non slot_guest_capacities Classic (create-booking/index.ts L368–439).
Valutare gap Classic: cap per-fascia in Impostazioni è in slot_guest_capacities, mentre il blocco edge legge service_slots.max_guests → potrebbe servire allineamento codice/migrazione, non solo toggle.
Segnare come evoluzione prodotto, non come configurazione self-service per Mario.

Fase E — Documentazione risposta a Mario (bozza messaggio)
Per limitare le prenotazioni dal sito, imposta Coperti max al giorno in Impostazioni e salva. È l’unico limite che il sito rispetta in automatico.

I Coperti max per fascia ti aiutano a vedere quando Pranzo o Cena sono pieni nel pannello e nel calendario, ma non impediscono al cliente di inviare la richiesta: decidi tu se accettarla o meno.

Contano solo le prenotazioni che hai già accettato; le richieste in attesa non occupano posto finché non le confermi.

2. LA CARD DI PROVENIENZA
Azione proposta o STOP:
  Guidare Mario a impostare daily_guest_limit in Admin → Impostazioni → «Coperti massimi al giorno»
  + salvataggio; spiegare che le caselle per-fascia sono solo semaforo admin, non blocco pubblico.
  Verifica E2E con submit su /prenota/:slug. STOP condizionale solo se Mario vuole blocco per-fascia sul sito.
Perché agisco così:
  La richiesta di Mario («non prenotino oltre dal sito») coincide con un solo meccanismo attivo oggi:
  il limite giornaliero server-side. Le caselle per-fascia sono volute come riferimento operativo.
Decisione/fonte citata:
  - RestaurantSettingsTab.tsx L1329–1340 — UI «Coperti massimi al giorno» + testo «la pagina pubblica non accetta nuove richieste»
  - RestaurantSettingsTab.tsx L1419–1423 — help fasce: «non blocca nulla in automatico»
  - create-booking/index.ts L349–366 — blocco pubblico DAILY_LIMIT su prenotazioni accepted
  - create-booking/index.ts L332–334, L368–369 — slot_limit_enabled default false, blocco per-fascia disattivato
  - ADMIN_PRENOTAZIONI_CONTEXT.md punti 10–11 — decisione Matteo 11-06-26: rimosso blocco per-fascia pubblico
  - ServiceSlotsManager.tsx L688–691 — Pro: «segnala… ma non blocca né rifiuta nulla»
  - ADMIN_SETTINGS_CONTEXT.md L130 — daily_guest_limit blocca solo Prenota pubblica; admin può sforare con avviso
Condizioni che coincidono:
  - Mario descrive esattamente le due UI presenti (giorno intero + per fascia).
  - Il codice conferma che solo daily_guest_limit è enforced sul pubblico con default attuale.
  - Help UI e doc admin sono allineati sul ruolo «morbido» delle capienze per-fascia.
Informazione che manca o confligge:
  - Edition del tenant (Classic vs Pro) → cambia solo DOVE si editano le cap per-fascia, non il blocco pubblico.
  - Valore attuale daily_guest_limit del tenant (vuoto = nessun blocco).
  - Aspettativa Mario: se vuole blocco automatico anche per fascia, confligge con prodotto corrente.
  - Le pending non contano verso il limite: può sorprendere un ristoratore.
Prossimo passo sicuro:
  Identificare tenant + leggere daily_guest_limit in DB; se vuoto/null, guidare Mario al Salva con valore > 0;
  poi test manuale submit pubblico fino a DAILY_LIMIT.
3. LO STOP
STOP minimo (una domanda):

«Mario, per il sito ti basta un tetto totale al giorno, oppure vuoi che ogni fascia (pranzo/cena) blocchi da sola le prenotazioni online?»

Perché: con il prodotto com’è oggi, solo il primo caso si risolve con la configurazione in Impostazioni. Il secondo richiederebbe riattivare slot_limit_enabled (non in UI, decisione prodotto) e verificare allineamento Classic slot_guest_capacities vs service_slots.max_guests nell’edge — non è self-service.

Se Mario risponde «basta il totale giornaliero» → nessuno STOP ulteriore: il percorso B1 + verifica C è sufficiente e documentato nel codice.